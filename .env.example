# MathPulse AI - Environment Variables Template
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# ============ Backend Configuration ============
# Hugging Face API Token (for AI features)
HF_TOKEN=your_huggingface_token_here

# ============ Frontend Configuration ============
# API URL (use http://backend:7860 for Docker internal, http://localhost:8000 for external)
VITE_API_URL=http://localhost:8000

# Firebase Configuration (already configured in firebase.ts, but can override here)
# VITE_FIREBASE_API_KEY=your_firebase_api_key
# VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
# VITE_FIREBASE_PROJECT_ID=your_project_id
# VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
# VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
# VITE_FIREBASE_APP_ID=your_app_id

# ============ Development Settings ============
# Set to 'true' for debug mode
DEBUG=false

# Node environment
NODE_ENV=development
